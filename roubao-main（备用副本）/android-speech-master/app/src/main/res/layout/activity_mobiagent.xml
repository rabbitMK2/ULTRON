<?xml version="1.0" encoding="utf-8"?>
<ScrollView
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="fill_parent"
    android:layout_height="fill_parent"
    android:fillViewport="true"
    android:background="@drawable/bg_mobiagent">

    <RelativeLayout
        android:layout_width="fill_parent"
        android:layout_height="wrap_content">

        <androidx.recyclerview.widget.RecyclerView
            android:id="@+id/recycler_view"
            android:layout_width="fill_parent"
            android:layout_height="fill_parent"
            android:layout_above="@+id/bottom_layout"
            android:layout_below="@+id/top_buttons_layout"
            android:layout_marginTop="8dp"
            android:layout_marginBottom="8dp"
            android:background="@android:color/transparent" />

        <!-- 顶部按钮布局容器 -->
        <RelativeLayout
            android:id="@+id/top_buttons_layout"
            android:layout_width="fill_parent"
            android:layout_height="wrap_content"
            android:layout_alignParentTop="true"
            android:layout_marginTop="16dp"
            android:paddingStart="16dp"
            android:paddingEnd="16dp">

            <Button
                android:id="@+id/clearButton"
                android:layout_width="90dp"
                android:layout_height="50dp"
                android:layout_alignParentStart="true"
                android:backgroundTint="@color/purple"
                android:text="清空"
                android:textColor="@color/white"
                android:textSize="14sp"
                android:gravity="center"
                android:singleLine="true"
                android:maxLines="1" />

            <Button
                android:id="@+id/shareButton"
                android:layout_width="90dp"
                android:layout_height="50dp"
                android:layout_alignParentEnd="true"
                android:backgroundTint="@color/purple"
                android:text="详情"
                android:textColor="@color/white"
                android:textSize="14sp"
                android:gravity="center"
                android:singleLine="true"
                android:maxLines="1" />

        </RelativeLayout>

        <TextView
            android:id="@+id/welcome_text"
            android:layout_width="fill_parent"
            android:layout_height="wrap_content"
            android:layout_centerInParent="true"
            android:gravity="center"
            android:text="我是你的智能手机助手\n如果要获取我的使用详情\n请点击右上角按钮（收回键盘）\n操作响应较慢，请耐心等待"
            android:textColor="#e61515ff"
            android:textSize="24dp"
            android:textStyle="bold" />

        <!-- 底部区域：上面一行为输入框+按钮，下面一行为长按语音按钮 -->
        <LinearLayout
            android:id="@+id/bottom_layout"
            android:layout_width="fill_parent"
            android:layout_height="wrap_content"
            android:layout_alignParentBottom="true"
            android:layout_marginBottom="10dp"
            android:orientation="vertical"
            android:padding="8dp">

            <!-- 第一行：终止 + 输入框 + 发送 -->
            <RelativeLayout
                android:layout_width="fill_parent"
                android:layout_height="80dp">

                <!-- 终止按钮 - 最左边 -->
                <Button
                    android:id="@+id/copy_button"
                    android:layout_width="70dp"
                    android:layout_height="48dp"
                    android:layout_alignParentStart="true"
                    android:layout_centerVertical="true"
                    android:layout_marginStart="8dp"
                    android:backgroundTint="@color/purple"
                    android:contentDescription="Terminate Task"
                    android:text="终止"
                    android:textColor="@color/white"
                    android:textSize="14sp"
                    android:textStyle="bold"
                    android:gravity="center"
                    android:singleLine="true"
                    android:maxLines="1" />

                <!-- 发送按钮 - 最右边 -->
                <Button
                    android:id="@+id/send_btn"
                    android:layout_width="70dp"
                    android:layout_height="48dp"
                    android:layout_alignParentEnd="true"
                    android:layout_centerVertical="true"
                    android:layout_marginEnd="8dp"
                    android:backgroundTint="@color/purple"
                    android:contentDescription="Send Message"
                    android:text="发送"
                    android:textColor="@color/white"
                    android:textSize="14sp"
                    android:textStyle="bold"
                    android:gravity="center"
                    android:singleLine="true"
                    android:maxLines="1" />

                <!-- 输入框 - 占据中间空间 -->
                <EditText
                    android:id="@+id/message_edit_text"
                    android:layout_width="fill_parent"
                    android:layout_height="wrap_content"
                    android:layout_toRightOf="@id/copy_button"
                    android:layout_toLeftOf="@id/send_btn"
                    android:layout_centerVertical="true"
                    android:layout_marginStart="8dp"
                    android:layout_marginEnd="8dp"
                    android:background="@drawable/rounded_corner"
                    android:textColor="#ff000000"
                    android:inputType="text"
                    android:padding="16dp" />

            </RelativeLayout>

            <!-- 第二行：底部长条按钮，左右贴边，像素层级最底部 -->
            <Button
                android:id="@+id/mobiHoldToTalkButton"
                android:layout_width="fill_parent"
                android:layout_height="72dp"
                android:layout_marginStart="0dp"
                android:layout_marginEnd="0dp"
                android:layout_marginTop="8dp"
                android:text="长按说话，松开发送到输入框"
                android:textSize="18sp"
                android:backgroundTint="@color/purple"
                android:textColor="@color/white" />

        </LinearLayout>

    </RelativeLayout>

</ScrollView>

